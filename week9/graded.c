#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void main(){
    // int* ptr = (int*)malloc(50);
    int* ptr = NULL;
    if (!ptr)
    {
        printf("%d", sizeof(int)*4);

    }
    // int arr[2][3] = {{1, 3, 5}, {2, 4, 6}};
    // int* ptr = &arr[0][1];
    // printf("%d", *(ptr+3));
    
}